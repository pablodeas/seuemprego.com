version: '3.8'

services:
  psql:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: ${USER}
      POSTGRES_PASSWORD: ${PASSWORD}
    ports:
      - '5432:5432'
    volumes:
      - psql-data:/var/lib/postgresql/data

volumes:
  psql-data: